# AZV ID Control

Система верификации пользователей для управления заявками на верификацию документов.

## Возможности

### 🔐 Аутентификация
- Страница входа в систему
- Регистрация новых пользователей
- Демо-доступ для тестирования

### 📋 Верификация документов
- **Единый список пользователей** с ролевой системой
- **Финансовая проверка** - с выбором класса допуска (A, AB, ABC)
- **МВД проверка** - стандартная верификация документов
- **Фильтрация по статусам:**
  - Новые заявки
  - Одобренные
  - Отклонённые
- **Фильтрация по ролям:**
  - Финансовая проверка
  - МВД

### 👤 Управление пользователями
- **Расширенная карточка пользователя** - все данные видны сразу
- **Профильная информация:**
  - Фото профиля
  - Имя и фамилия
  - Статус верификации
  - Время последней активности
  - Рейтинг и количество отзывов
  - Роли (Финансовая проверка, МВД)
- **Контактные данные:**
  - Телефон
  - ИИН
  - Номер паспорта
  - Дата подачи заявки
- **Превью документов** - миниатюры всех документов
- **Детальный просмотр документов:**
  - Удостоверение личности (лицевая и оборотная стороны)
  - Водительское удостоверение
  - Селфи (портрет)
  - Селфи с водительским удостоверением
- **Действия модератора:**
  - Одобрение/отклонение заявок
  - Выбор класса допуска для финансовой проверки
  - Указание причины отклонения

### 📊 Аналитика
- Статистика по заявкам
- Счетчики по статусам
- Поиск и фильтрация

## Технологии

- **Next.js 15** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **Class Variance Authority** - управление вариантами компонентов

## Установка и запуск

1. Установите зависимости:
```bash
npm install
# или
pnpm install
```

2. Запустите проект в режиме разработки:
```bash
npm run dev
# или
pnpm dev
```

3. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## Демо-доступ

Для входа в систему используйте:
- **Логин:** admin
- **Пароль:** admin123

## Структура проекта

```
azv-idcontrol/
├── app/                    # Next.js App Router
│   ├── login/             # Страница входа
│   ├── register/          # Страница регистрации
│   ├── verification/      # Главная страница верификации
│   └── page.tsx           # Главная страница
├── components/            # React компоненты
│   ├── ui/               # Базовые UI компоненты
│   └── verification/     # Компоненты верификации
├── lib/                  # Утилиты и данные
│   ├── utils.ts          # Общие утилиты
│   └── mock-data.ts      # Тестовые данные
├── types/                # TypeScript типы
│   └── verification.ts   # Типы для верификации
└── README.md
```

## Особенности дизайна

- **Адаптивный дизайн** - работает на desktop и мобильных устройствах
- **Современный UI** - в стиле azv-webview
- **Темная/светлая тема** - автоматическое переключение
- **Интуитивная навигация** - простое управление

## Компоненты

### UI Components
- `Button` - кнопки с различными вариантами
- `Input` - поля ввода с валидацией
- `Card` - карточки для контента
- `Tabs` - вкладки для навигации

### Verification Components
- `UserCard` - карточка пользователя с действиями
- `DocumentViewer` - просмотрщик документов

## API (Mock)

Приложение использует mock данные для демонстрации. В реальном проекте необходимо:

1. Создать API endpoints для:
   - Аутентификации
   - Получения списка пользователей
   - Одобрения/отклонения заявок
   - Загрузки документов

2. Интегрировать с реальной базой данных

3. Добавить систему ролей и разрешений

## Развертывание

Для продакшена:

```bash
npm run build
npm start
```

Или используйте Docker:

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## Лицензия

© 2024 AZV ID Control. Все права защищены.